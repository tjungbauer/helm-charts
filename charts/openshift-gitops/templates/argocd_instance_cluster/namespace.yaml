{{ if eq (.Values.gitopsclusterinstance.namespace.create | toString) "true" }}
---
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.gitopsclusterinstance.namespace.name | quote }}
  labels:
    openshift.io/cluster-monitoring: "true"
    {{- include "common.labels" $ | nindent 4 }}
{{- end }}
