Fehler: Unbekannter Schalter `1'
Fehler: Unbekannter Schalter `1'
Verwendung: git fetch [<Optionen>] [<Repository> [<Refspec>...]]
      oder: git fetch [<Optionen>] <Gruppe>
      oder: git fetch --multiple [<Optionen>] [(<Repository> | <Gruppe>)...]
      oder: git fetch --all [<Optionen>]

    -v, --[no-]verboseVerwendung: git fetch [<Optionen>] [<Repository> [<Refspec>...]]
      oder: git fetch [<Optionen>] <Gruppe>
      oder: git fetch --multiple [<Optionen>] [(<Repository> | <Gruppe>)...]
      oder: git fetch --all [<Optionen>]

    -v, --[no-]verbose    erweiterte Ausgaben
    -q, --[no-]quiet      weniger Ausgaben
    --[no-]all    erweiterte Ausgaben
    -q, --[no-]quiet      weniger Ausgaben
    --[no-]all            fordert von allen Remote-Repositories an
    --[no-]set-upstream            fordert von allen Remote-Repositories an
    --[no-]set-upstream   Upstream für "git pull/fetch" setzen
    -a, --[no-]append   Upstream für "git pull/fetch" setzen
    -a, --[no-]append     an .git/FETCH_HEAD anhängen statt zu überschreiben
    --[no-]atomic     an .git/FETCH_HEAD anhängen statt zu überschreiben
    --[no-]atomic         atomare Transaktionen nutzen, um Referenzen zu aktualisieren
    --[no-]upload-pack         atomare Transaktionen nutzen, um Referenzen zu aktualisieren
    --[no-]upload-pack <Pfad> <Pfad>
                          Pfad des Programms zum Hochladen von Paketen auf der Gegenseite
    -f, --[no-]force      das Überschreiben einer lokalen Referenz erzwingen
    -m, --[no-]multiple
                          Pfad des Programms zum Hochladen von Paketen auf der Gegenseite
    -f, --[no-]force      das Überschreiben einer lokalen Referenz erzwingen
    -m, --[no-]multiple   von mehreren Remote-Repositories anfordern
    -t, --[no-]tags          von mehreren Remote-Repositories anfordern
    -t, --[no-]tags       alle Tags und verbundene Objekte anfordern
    -nalle Tags und verbundene Objekte anfordern
    -n                    nicht alle Tags anfordern (--no-tags)
                    nicht alle Tags anfordern (--no-tags)
    -j, --[no-]jobs    -j, --[no-]jobs <Anzahl>
                          Anzahl der parallel anzufordernden Submodule
    --[no-]prefetch <Anzahl>
                          Anzahl der parallel anzufordernden Submodule
    --[no-]prefetch       Refspec verändern, damit alle Referenzen unter refs/prefetch/ platziert werden
    -p, --[no-]prune       Refspec verändern, damit alle Referenzen unter refs/prefetch/ platziert werden
    -p, --[no-]prune      Remote-Tracking-Branches entfernen, die sich nicht mehr im Remote-Repository befinden
    -P, --[no-]prune-tags lokale Tags entfernen, die sich nicht mehr im Remote-Repository befinden, und geänderte Tags aktualisieren
    --[no-]recurse-submodules      Remote-Tracking-Branches entfernen, die sich nicht mehr im Remote-Repository befinden
    -P, --[no-]prune-tags lokale Tags entfernen, die sich nicht mehr im Remote-Repository befinden, und geänderte Tags aktualisieren
    --[no-]recurse-submodules[=<bei-Bedarf>]
                          [=<bei-Bedarf>]
                          rekursive Anforderungen von Submodulen kontrollieren
    --[no-]dry-run        Probelauf
    --[no-]porcelainrekursive Anforderungen von Submodulen kontrollieren
    --[no-]dry-run        Probelauf
    --[no-]porcelain      maschinenlesbare Ausgabe
    --[no-]write-fetch-head      maschinenlesbare Ausgabe
    --[no-]write-fetch-head
                          schreibe angeforderte Referenzen in die FETCH_HEAD-Datei
    -k, --[no-]keep
                          schreibe angeforderte Referenzen in die FETCH_HEAD-Datei
    -k, --[no-]keep       heruntergeladenes Paket behalten
    -u, --[no-]update-head-ok       heruntergeladenes Paket behalten
    -u, --[no-]update-head-ok
                          Aktualisierung der "HEAD"-Referenz erlauben
    --[no-]progress       Fortschrittsanzeige erzwingen
    --[no-]depth <Tiefe>  die Historie eines Klons mit unvollständiger Historie (shallow) vertiefen
    --[no-]shallow-since <Zeit>
                          die Historie eines Klons mit unvollständiger Historie (shallow) auf Zeitbasis
                          vertiefen
    --[no-]shallow-exclude <Referenz>
                          Historie eines flachen Klons vertiefen, Referenz exkludiert
    --[no-]deepen <Anzahl>
                          die Historie eines Klons mit unvollständiger Historie (shallow) vertiefen
    --unshallow           zu einem vollständigen Repository konvertieren
    --refetch             erneutes Abrufen ohne Aushandeln gemeinsamer Commits
    --[no-]update-shallow Referenzen, die .git/shallow aktualisieren, akzeptieren
    --refmap <Refmap>     Refmap für 'fetch' angeben
    -o, --[no-]server-option <serverspezifisch>
                          Aktualisierung der "HEAD"-Referenz erlauben
    --[no-]progress       Fortschrittsanzeige erzwingen
    --[no-]depth <Tiefe>  die Historie eines Klons mit unvollständiger Historie (shallow) vertiefen
    --[no-]shallow-since <Zeit>
                          die Historie eines Klons mit unvollständiger Historie (shallow) auf Zeitbasis
                          vertiefen
    --[no-]shallow-exclude <Referenz>
                          Historie eines flachen Klons vertiefen, Referenz exkludiert
    --[no-]deepen <Anzahl>
                          die Historie eines Klons mit unvollständiger Historie (shallow) vertiefen
    --unshallow           zu einem vollständigen Repository konvertieren
    --refetch             erneutes Abrufen ohne Aushandeln gemeinsamer Commits
    --[no-]update-shallow Referenzen, die .git/shallow aktualisieren, akzeptieren
    --refmap <Refmap>     Refmap für 'fetch' angeben
    -o, --[no-]server-option <serverspezifisch>
                          Option übertragen
    -4, --ipv4            nur IPv4-Adressen benutzen
    -6, --ipv6
                          Option übertragen
    -4, --ipv4            nur IPv4-Adressen benutzen
    -6, --ipv6            nur IPv6-Adressen benutzen
    --[no-]negotiation-tip <Commit>            nur IPv6-Adressen benutzen
    --[no-]negotiation-tip <Commit>
                          ausgeben, dass wir nur Objekte haben, die von diesem Objekt aus erreichbar sind
    --[no-]negotiate-only
                          ausgeben, dass wir nur Objekte haben, die von diesem Objekt aus erreichbar sind
    --[no-]negotiate-only keine Packdatei anfordern; stattdessen die Vorgänger der Verhandlungstipps anzeigen
    --[no-]filter keine Packdatei anfordern; stattdessen die Vorgänger der Verhandlungstipps anzeigen
    --[no-]filter <Argumente> <Argumente>
                          Filtern nach Objekten
    --[no-]auto-maintenance
                          Filtern nach Objekten
    --[no-]auto-maintenance
                          führe 'maintenance --auto' nach "fetch" aus
    
                          führe 'maintenance --auto' nach "fetch" aus
    --[no-]auto-gc        führe 'maintenance --auto' nach "fetch" aus
    --[no-]show-forced-updates--[no-]auto-gc        führe 'maintenance --auto' nach "fetch" aus
    --[no-]show-forced-updates
                          Prüfe auf erzwungene Aktualisierungen in allen aktualisierten Branches
    
                          Prüfe auf erzwungene Aktualisierungen in allen aktualisierten Branches
    --[no-]write-commit-graph
                          --[no-]write-commit-graph
                          Schreibe den Commit-Graph nach "fetch"
    --[no-]stdinSchreibe den Commit-Graph nach "fetch"
    --[no-]stdin          akzeptiere Refspecs von der Standard-Eingabe

          akzeptiere Refspecs von der Standard-Eingabe

